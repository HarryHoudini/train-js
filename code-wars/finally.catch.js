const fun1 = new Promise(() => {
    throw 1;
});
// Тут вываливается ошибка потому что catch cоздает паралельнный процесс к текущему промису и не перехватывает ошибку.
//  Вероятно потому что при создании промис выполняется в callback query
fun1.catch((e)=>{})
fun1.finally(()=> {console.log('fin')})
